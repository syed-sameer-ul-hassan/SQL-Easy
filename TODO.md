# SQLeasy Development Targets (TODO LIST)

### Core Integration & Workflow (Completed)
- [x] Create a standalone global system terminal application identity (`sqleasy`).
- [x] Build automated pre-flight dependency system validation logic.
- [x] Link passive asset collection architecture via `subfinder` routines.
- [x] Configure query endpoint crawl layers utilizing headless engine tracking via `katana`.
- [x] Establish interactive console target vector selection menu arrays mimicking a `wifite` UX blueprint.
- [x] Formulate a centralized automated payload passing module connecting targets straight into `sqlmap`.
- [x] Implement runtime keyboard signaling crash traps (`Ctrl+C`) to clean temp system files during rapid exit.

### Core Scan Upgrades (Pending)
- [ ] Incorporate `httpx` parsing to discover web services running across custom port matrices (`8080`, `8443`, `8000`).
- [ ] Implement advanced regex string processing routines to actively prioritize target metrics by high-probability variables (e.g., `?id=`, `?file=`, `?p=`).
- [ ] Add explicit multi-threading constraints inside native process pipelines to scale asset capture concurrency rates.

### Stealth Features (Pending)
- [ ] Add proxy routing array configuration handlers (`--proxy-file`) to escape simple gateway thread blocks.
- [ ] Setup customizable application interaction delay interval options to lower packet collision fingerprints.
- [ ] Deploy custom response tracking maps to alert users if a target security firewall begins throwing traffic drop rules.

### Logging Outputs (Pending)
- [ ] Auto-save successfully confirmed injection parameters into a persistent logs catalog (`logs/`).
- [ ] Create an injection summary export engine parsing completed tool targets out to raw cross-compatible data sheet frames (`.csv`).
